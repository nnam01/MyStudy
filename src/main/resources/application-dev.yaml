spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:13306/MYSTUDY
    username: ${AWS_DB_ID}
    password: ${AWS_DB_PASSWORD}

logging:
  level:
    org.springframework.web: DEBUG